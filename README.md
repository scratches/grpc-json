# Spring gRPC JSON

This project provides JSON encoding and decoding for gRPC services in Spring Boot applications. It allows you to expose gRPC services over HTTP/JSON, making it easier to integrate with web clients and other systems that communicate using JSON.